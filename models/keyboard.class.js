/**
 * Represents the state of key inputs for controlling the character.
 * It tracks whether specific keys (LEFT, RIGHT, UP, SPACE) are pressed or specific buttons are touched.
 */
class Keyboard {

    LEFT = false;
    RIGHT = false;
    UP = false;
    SPACE = false;

}